if(@refresh)
  page.replace_html @widget.global_id, @widget.render_template(self, @refresh)
else
  # This sends the remote call to the widget, which may then modify the page
  @widget.send("#{@action}_remotecall", page)
end
