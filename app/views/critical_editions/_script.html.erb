<script> 
    window.onresize = function () {
        set_height (); 
    }
    document.observe('dom:loaded', function() {
        set_height();        
        go_to_anchor('<%= @href_for_text %>');
    });
</script>
